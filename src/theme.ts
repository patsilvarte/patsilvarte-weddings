import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  typography: {
    fontFamily: "'Questrial', sans-serif",
    h1: {
      fontFamily: "'Hello January', cursive",
    },
    h2: {
      fontFamily: "'Hello Paris', serif",
    },
  },
});

export default theme;