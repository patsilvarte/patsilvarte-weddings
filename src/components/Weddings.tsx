import { Navbar } from "./Navbar";
import { WeddingsList } from "./WeddingsList";

export const Weddings = () => {
  return (
    <div>
      <Navbar visible />
      <WeddingsList />
    </div>
  );
};
